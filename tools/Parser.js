class Parser{
    static parse(codeStringList){
        let codeContainerStack=new Array();
        let blockStack=new Array();
        this.codeStringList=codeStringList;
    }

    static _parse(currentCodeString, remainingCodeString, codeContainerStack, blockStack){
        
    }
}